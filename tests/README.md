# Cross-component tests

- `unit`: logic độc lập framework.
- `integration`: database, filesystem, MinIO, Delta và publish boundary.
- `data`: ingestion, transformation, DQ, reconciliation, leakage và reproducibility.
- `e2e`: source-to-dashboard và source-to-ML-score.

