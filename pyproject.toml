[project]
name = "lakehouse-workspace"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = [
  "services/ecommerce-api",
  "generator",
]
