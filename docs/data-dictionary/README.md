# Data dictionary

Data dictionary bao phủ MySQL source, structured access log, Landing metadata, Bronze/Silver/Gold Iceberg, Trino serving views và ML datasets. Không copy password/hash, token, raw IP hoặc customer PII không cần thiết vào trusted downstream catalogue.

