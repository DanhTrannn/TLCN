# Seeds

Seed chỉ chứa category, product, variant, `opening_on_hand`/`on_hand` ban đầu và coupon demo `WELCOME10`. Dữ liệu lịch sử phải do `generator/` tạo để giữ seed, scenario và manifest tái lập.

